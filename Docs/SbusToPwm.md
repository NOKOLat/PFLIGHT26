## 手動操縦の処理

プログラム側は標準の最大、中立、最小を持っている（360, 1024, 1680）
プロポサイドが、送る値を調整（トリムやサブトリム）をすることで、実際にサーボに送られる値を変更できる
`sbus_rescaler`の中で、360 ~ 1680を0 ~ 100%として、プロポからの信号を変換 → サーボやモーターに送ればよい

## 自動操縦の処理

プログラム側で、トリムやサブトリムを反映させる必要がある
出力 = 標準の値(1024) + 角度（制御結果）*  定数（角度->PWM） + サブトリム（設定値）
